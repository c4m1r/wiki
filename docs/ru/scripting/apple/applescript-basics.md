# Знакомство с AppleScript

AppleScript позволяет управлять программами macOS с помощью сценариев.

Простой пример:
```applescript
display dialog "Привет" buttons {"OK"}
```
Сохраните код в файле `.scpt` и запустите через Script Editor.
